<h2 class="mb-4">{{ __( 'Reakce na poptávku', 'shp-partneri' ) }}</h2>

{% if post.status != 'expired' %}

  <form id="contactForm" data-ajax-action="request_message">

    <div class="row">

      <div class="col-md-6">
        <div class="form-group">
          <label class="required-asterisk" for="contactFormName">
            {{ __( 'Jméno a příjmení', 'shp-partneri' ) }}
          </label>
          <input type="text" name="name" class="form-control" id="contactFormName" required>
        </div>
      </div>

      <div class="col-md-6">
        <div class="form-group">
          <label class="required-asterisk" for="contactFormEmail">
            {{ __( 'E-mail', 'shp-partneri' ) }}
          </label>
          <input type="email" name="email" class="form-control" id="contactFormEmail" required>
        </div>
      </div>

    </div>

    <div class="form-group">
      <label class="required-asterisk" for="contactFormMessage">
        {{ __( 'Text reakce', 'shp-partneri' ) }}
      </label>
      <textarea class="form-control" name="message" rows="10" id="contactFormMessage" required></textarea>
    </div>

    <input type="hidden" name="post_id" value="{{ post.ID }}">

    <div class="g-recaptcha mb-3" data-sitekey="{{ config.G_RECAPTCHA_SITE_KEY }}"></div>

    <p class="text-danger d-none" id="contactFormError"></p>

    <p class="text-success form-control-plaintext font-weight-bold d-none fs-120" id="contactFormSuccess"></p>

    <button type="submit" class="btn btn-primary btn-lg">
      {{ __( 'Odeslat', 'shp-partneri' ) }}
    </button>

  </form>

{% else %}

  <p>
    {{ __( 'Poptávka byla zadávajícím označena jako vyřízena, nelze na ni již reagovat.
      Rychlejší přístup k&nbsp;novým objednávkám si můžete zajistit členstvím v&nbsp;Shoptet
      Partneři, které je zdarma. Shoptet Partnerům zasíláme poptávky 24 hodin před
      zveřejněním na webu.'
      , 'shp-partneri' ) }}
  </p>

{% endif %}