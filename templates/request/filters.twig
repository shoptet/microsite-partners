<div class="filter mb-4 mb-md-0">
  <button
    class="btn btn-primary d-md-none"
    type="button"
    data-toggle="collapse"
    href="#filtersCollapse"
    role="button"
    aria-expanded="false"
    aria-controls="filtersCollapse"
  >
    {{ __( 'Zobrazit filtry', 'shp-obchodiste' ) }}
  </button>

  <div class="collapse d-md-block" id="filtersCollapse">

    <div class="mt-3 mt-md-0">
      <p class="h5 font-weight-bold mt-0 mb-2">
        {{ __( 'Kategorie', 'shp-obchodiste' ) }}
      </p>

      <ul class="list-unstyled mb-0">
        <li>
          <a class="filter-link" href="{{ link.archive.request }}">
            {{ __( 'Všechny poptávky', 'shp-obchodiste' ) }}
            <span class="text-semilight">({{ request_posts_count.publish }})</span>
          </a>
        </li>
        {% for t in terms %}
          <li>
            <a class="filter-link {% if t.id == term.id %}filter-link-active{% endif %}" href="{{ t.link }}">
              {{ t.name }}
              <span class="text-semilight">({{ t.count }})</span>
            </a>
          </li>
        {% endfor %}
      </ul>

    </div>
  </div>
</div>