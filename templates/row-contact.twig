<div class="row-badges row-breadcrumb-bg pt-5 pb-5" id="contact">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-sm-12 col-md-6">
                {% if locale == 'cs_CZ' %}
                    <div class="text-center">
                        <h2>{{ __( 'Kontakt', 'shp-partneri' ) }}</h2>
                        <p>Omlouváme se, ale aktuálně nepřijímáme nové žádosti o partnerství a to až do 30. 9. 2023.</p>
                        <p>Prosíme, zaregistrujte se až po tomto datu. Děkujeme za pochopení.</p>
                    </div>
                {% else %}
                    <h2>{{ __( 'Kontakt', 'shp-partneri' ) }}</h2>
                    {{ options.themeComplaintContactText }}
                {% endif %}
            </div>
            {% if locale != 'cs_CZ' %}
                <div class="col-sm-12 col-md-6">
                    {% if read_only_enabled %}
                        {% do action('read_only_fallback') %}
                    {% else %}
                        {% include 'contact-form.twig' %} 				
                    {% endif %}
                </div>
            {% endif %}
        </div>
    </div>
</div>
